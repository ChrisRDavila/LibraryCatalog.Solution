@{
  Layout = "_Layout";
}

@using Library.Models;

<h1>All Checkouts</h1>

@if (@Model.Count == 0)
{
  <h3>There are no checkouts yet</h3>
} 
else
{
  <ul>
    @foreach (Checkout checkout in Model)
    {
      <li>@Html.ActionLink($"{checkout.CheckoutId}{checkout.CheckoutDate}", "Details", new { id = checkout.CheckoutId })</li>
    }
  </ul>
}

<p>@Html.ActionLink("Create a Checkout", "Create")</p>