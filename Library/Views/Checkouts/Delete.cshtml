@{
  Layout = "_Layout";
}

@model Library.Models.Checkout

<h2>Are you sure you want to remove this checkout?</h2>

@Html.DisplayNameFor(model => model.CheckoutId): @Html.DisplayFor(model => model.CheckoutId) | @Html.DisplayFor(model => model.CheckoutDate)</h4>)
@using (Html.BeginForm())
{
  <input type="submit" class="button" value="Delete" />
}